package chap06.textbook.s061302.package2;

import chap06.textbook.s061302.package1.A;

public class C {
	//필드
	A a1 = new A(true); 	// (o)
	//A a2 = new A(1);		// (x) default 접근 제한자 (다른 패키지)
	//A a3 = new A("문자열");	// (x) private 접근 제한자 (다른 클래스)
	}

